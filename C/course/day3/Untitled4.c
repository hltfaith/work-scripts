#include <stdio.h>

int main(void)
{
    /*
    // getch函数，也是接收键盘输入的一个字符，但不用回车确认！使用时要包含conio的头文件。
    char ch;
    ch = getch();
    printf("%c\n", ch);


    printf("test......");


    // getche函数，同样接收一个字符，也在conio头文件中声明，也不需要回车确认！但输入时明文显示（回显）
    char ch;
    ch = getche();
    printf("%c\n", ch);


    // 基本运算符
    int a, b;
    a=b=5;
    printf("%d      %d\n", --a, --b);
    printf("%d      %d\n", a--, --b);
    printf("%d      %d\n", a--, --b);
    printf("%d      %d\n", a--, --b);



    // 案例2
    //两个%才会输出一个%
    int a = 20;
    int b = 5;
    int c = 6;
    printf("a = %d b = %d c = %d\n", a, b, c);
    printf("a + b = %d\n", a+b);
    printf("a - c = %d\n", a-c);
    printf("a / c = %d\n", a/c);
    printf("a %% c = %d\n", a%c);

    // 其他运算符
    int n = 0;
    int intsize = sizeof(int);
    printf("int sizeof is %d bytes\n",intsize);

    // VC6断点调试技巧<第一篇>
    int i = 0;
    while(i < 100)
    {
        printf("changhao,hello!");
        i++;
    }



    // if 判断
    int flag = 100;
    if( flag >= 90)
    {
        printf("Good\n");
    }
    else if (flag >= 60)
    {
        printf("passed");
    }
    else
    {
        printf("failed\n");
    }

    //switch 语句
    int value = 10;
    switch(value)
    {
        case 1: printf("one"); break;
        case 2: printf("two"); break;
        case 3: printf("three"); break;
        default: printf("other"); break;
        default: printf("other"); break;
    }



    // 循环结构
    // C语言提供三种循环结构，分别为while循环,do while循环和for循环。

    //while 语句
    int i = 1;
    while(i++<10)
    {
        printf("count %d", i);
    }

    // do while循环
    int i = 1;
    do
    {
        printf("count %d", i);
        i++;
    }while(i<20);

        */
    for(int i=0; i<100; i++)
    {
        printf("i count is %d\n", i);
    }





























    return 0;
}
































